What patterns or surprises did you notice?
I noticed that the functions all have the same pattern using the parameter to pass down "n" and then calculate inside the function using return

What logic challenged your thinking?
The logic that challenged my thinking the most was using "%" to check whether a number is even or odd.

How might this kind of toolkit be used in the real world?
This kind of toolkit could be used in the real world to organize reusable functions.